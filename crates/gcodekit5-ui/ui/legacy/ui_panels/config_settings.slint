// Legacy config settings panel - archived
import { VerticalBox, ScrollView } from "std-widgets.slint";

export component ConfigSettingsPanel inherits Rectangle {
    // archived stub
}

export struct ConfigSetting { name: string, value: string }
