// Archived copy of gcode_editor.slint
// This file was moved from ui_panels/gcode_editor.slint for archival and migration purposes.
import { Button, LineEdit, VerticalBox, HorizontalBox } from "std-widgets.slint";
import { CustomTextEdit } from "../ui/ui_components/custom_text_edit.slint";
import { Theme } from "../ui/theme.slint";
import { StandardButton, StandardSidebar } from "../ui/ui_components/shared.slint";

export struct TextLine {
    line_number: int,
    content: string,
    is_dirty: bool,
}

export component GcodeEditorPanel inherits Rectangle {
    // body omitted for archival copy
}
