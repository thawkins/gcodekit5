// Legacy materials manager panel - archived
import { VerticalBox } from "std-widgets.slint";

export component MaterialsManager inherits Rectangle {
    // archived stub
}

export struct MaterialData { id: int, name: string }
