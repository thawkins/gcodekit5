// Legacy Machine Control Panel (archived)
import { Button, ComboBox, HorizontalBox, VerticalBox } from "std-widgets.slint";

export component MachineControlPanel inherits Rectangle {
    // archived stub - functionality moved to GTK4 implementation
}
