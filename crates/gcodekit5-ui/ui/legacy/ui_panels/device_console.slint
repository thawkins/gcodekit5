// Legacy Device Console Panel (archived)
// Minimal stub to preserve import path during archival.
import { TextEdit } from "std-widgets.slint";

export component DeviceConsolePanel inherits Rectangle {
    // archived stub - functionality moved to GTK4 implementation
}
