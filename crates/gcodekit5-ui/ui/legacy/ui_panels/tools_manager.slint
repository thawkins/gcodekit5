// Legacy tools manager - archived
import { VerticalBox } from "std-widgets.slint";

export component ToolsManager inherits Rectangle {
    // archived stub
}

export struct ToolData { id: int, name: string }
