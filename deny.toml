# cargo-deny configuration for dependency auditing
# Run: cargo deny check
# Docs: https://embarkstudios.github.io/cargo-deny/

[graph]
targets = []
all-features = false
no-default-features = false

[output]
feature-depth = 1

[advisories]
# Permitimos paquetes obsoletos de terceros para saltarnos las 3 versiones de ttf-parser
unmaintained = "none"
ignore = [
    "RUSTSEC-2024-0436",
    "RUSTSEC-2021-0140",
    "RUSTSEC-2026-0244",
]

[licenses]
allow = [
    "MIT", "Apache-2.0", "Apache-2.0 WITH LLVM-exception", "BSD-2-Clause",
    "BSD-3-Clause", "ISC", "Zlib", "Unicode-3.0", "Unicode-DFS-2016",
    "BSL-1.0", "CC0-1.0", "MPL-2.0", "OpenSSL", "EUPL-1.2", "NCSA",
]
confidence-threshold = 0.8
exceptions = []

[licenses.private]
ignore = true
registries = []

[bans]
multiple-versions = "allow"
wildcards = "allow"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
allow = []
deny = []
skip = []
skip-tree = []

[sources]
unknown-registry = "allow"
unknown-git = "allow"
allow-registry = ["https://github.com"]
allow-git = []

[sources.allow-org]
github = []
gitlab = []
bitbucket = []

